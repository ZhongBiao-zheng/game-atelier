# 2026-05-18 · game-ui-ai-workflow

## 主线
1. **writing-plans 实施计划 v1 落定**：`docs/plans/2026-05-18 …-实施计划-v1.md`，5 Lane 并行架构（A schema 阻塞先行 → B Skill / C FastAPI / D React 三路并行 → E 集成），~26 task，TDD + 每 task 独立 commit。预计模型执行单位 ~10-13h CC。
2. **3 Skill 协作模型确定**：自研 2 个（`character-workflow` 主入口 + `viewer-server` mini-skill），外部锁定 1 个（`lovart-api` via skills-lock.json）。viewer-server 是主 Skill 启动的 FastAPI server，不是画师直接调用的入口。
3. **功能说明.html 改造为 PPT 风格演示**：15 张 scroll-snap 幻灯片（封面 / 痛点 / 三前提推翻 / one-liner / 架构 / 三栏 UI / 剪贴板桥接 / 工作循环 demo / 数据契约 / 3 Skill / 5 Lane / 技术栈 / 边界风险 / 验收时间线 / 收尾）；5 个内嵌 SVG 插图（架构图 / UI mockup / Skill 协作 / Lane 依赖 / 封面几何）。沿用既有 token（深色 + 紫色 #7c5af7 + Syne/IBM Plex），加键盘导航和进度条。

## 关键决策
- **plan 输出路径覆盖 skill 默认**：用户全局 CLAUDE.md 规定 `docs/plans/YYYY-MM-DD 名.md`，writing-plans skill 的 `docs/superpowers/plans/` 默认被覆盖。skill 文档明示 "User preferences for plan location override this default"。
- **schema 必须双端手工同步**：T1 原话"Skill 和 React 组件均 import 同一 interface"是不可能的（Python ↔ TS 跨语言）。实际方案：`schemas.py`（Pydantic）+ `jobs.ts`（TS interface）+ `docs/api-contract.md`（文档级同步源）。
- **plan 自审发现 §5.4 违规并修复**：SpecForm 的 useEffect 在 sseSignal 变化时无脑覆盖 content/dirty，违反"不自动覆盖画师未保存草稿"。已改为只更新 serverContent 驱动 stale notice，content 仅在 `!dirty` 时刷新。

## 底层规律
- **plan 大文档自审三步**：spec coverage（每个需求都有对应 task 吗）+ placeholder scan（TBD / "implement later" 红旗）+ type consistency（方法签名跨 task 一致吗）。本轮自审 catch 到一处 §5.4 违规。
- **fresh start 项目要锁 schema 契约先行**：4 个子系统（schema / Skill / Server / Web）紧耦合时，schema 是首要瓶颈。Lane A 先跑通（~30 min），Lane B/C/D 才能并行；schema 不稳则三路相互撞车。
- **PPT 风格 HTML 的节奏感来自 scroll-snap-y mandatory**：每个 section `min-height: 100vh` + `scroll-snap-align: start`，配合 IntersectionObserver 自动更新页码，比写 reveal.js 框架轻得多。

## Memory 更新
- 全局月志已追加。
- session-count: 3/5 → 4/5。
