# 创作台统一计价器

## 目标

创作台在提交图片或视频时，对已核实价目计算人民币预计费用并冻结到 Job；生成成功后，在历史记录的“耗时 · 时间”一行最右侧展示。未核实价格的渠道、模型或参数不写费用，也不显示占位提示。

## 范围

- 统一以真实渠道、协议、模型和本次生成参数计算预计费用。
- 支持已核实价目的 OpenAI-HK 图片、火山 Ark Seedream / Seedance、阿里百炼 HappyHorse。
- 转售渠道只使用自身已核实价目；TokenDance、Tuzi、OpenRouter 等无法可靠推导时不展示费用。
- 图片费用乘以生成张数；视频费用按对应厂商规则乘以时长、输出规格和生成条数。
- Seedance 仅在分辨率、比例和时长可确定，且没有未知时长的参考视频时计算。
- 完整创作台与首页紧凑创作台共用同一提交计价入口。
- 提交时将预计费用写入 `params.estimated_cost_cny`；历史只读取该快照，不按当前价目回算旧任务。
- 输入区不显示提交前价格；历史金额统一以 `¥ 10.11` 样式显示，最多两位小数且不补无意义的零。
- 本功能不是厂商实际账单，不实现余额查询、扣费对账或用户自定义价目。

## 验收标准

1. 已核实价目的生成任务在提交时保存人民币费用快照，未核实价目不保存费用字段。
2. 同模型经不同渠道调用时不会错误复用官方直连价格。
3. 完整创作台与首页紧凑创作台使用同一计价规则，图片和视频均覆盖。
4. 成功历史在耗时/时间行最右侧显示 `¥ **.**`；无法计算、旧任务无快照时不显示。
5. 历史展示只读取提交快照，价目更新不会改变旧记录。
6. 已核实价格、渠道防串价、格式化、费用冻结与历史显隐均有自动化测试。
7. 删除旧的 OpenAI-HK 专用计价入口，只保留统一计价器。

## 价格来源

- 火山方舟产品定价页，2026-08-27 核对。
- 阿里云百炼模型定价页，2026-08-27 核对。
- OpenAI-HK 与 Tuzi 由用户按实际渠道账单确认，2026-08-28 核对。

## 统一换算

- 只有美元报价需要换算时，固定使用 `1 USD = ¥7`。
- 汇率不联网、不动态更新；提交时折算成人民币并冻结到 Job。

## 已核实价格（2026-08-28）

### OpenAI-HK

均按最终生成张数计费，不受比例、透明背景或其他参数影响：

| 模型 | 档位 | 人民币单价 |
|---|---|---:|
| GPT Image 2 | 全部 | ¥0.08 / 张 |
| Nano Banana | 全部 | ¥0.20 / 张 |
| Nano Banana 2 | 基础 | ¥0.48 / 张 |
| Nano Banana 2 | 2K | ¥0.72 / 张 |
| Nano Banana 2 | 4K | ¥1.00 / 张 |
| Nano Banana HD | 全部 | ¥0.32 / 张 |

Nano Banana 2 的基础 / 2K / 4K 必须按真实请求字段映射；实现前需核对当前 UI 的 `quality` 与上游分辨率语义，不能只凭中文标签猜档位。

### Tuzi

Tuzi 价格依赖账号所在计费分组。当前用户属于 `default`；系统必须显式保存计费分组，不能把 `default` 价格套给其他用户。

`default` 分组已核实：

| 模型 | 档位 | 人民币单价 |
|---|---|---:|
| GPT Image 2 | 全部 | ¥0.035 / 张 |
| Seedream 4.5 | 全部 | ¥0.12 / 张 |
| Seedream 5.0 Pro | 全部 | ¥0.60 / 张 |
| Nano Banana Pro | 基础 | ¥0.072 / 张 |
| Nano Banana Pro | 2K | ¥0.32 / 张 |
| Nano Banana Pro | 4K | ¥0.35 / 张 |
| Nano Banana 2 | 基础 | ¥0.30 / 张 |
| Nano Banana 2 | 2K | ¥0.48 / 张 |
| Nano Banana 2 | 4K | ¥0.82 / 张 |
| Midjourney `mj_imagine` | 全部 | ¥0.1505 / 任务 |

“绘画”分组目前只核实 GPT Image 2：`¥0.21 / 张`；其他模型在该分组的价格仍未知。

Midjourney 按任务计费：一次 imagine 即使返回四张拆分结果，也只记一笔 `¥0.1505`，不能按输出张数乘四。

### TokenDance

图片模型当前记录：

| 模型 | 价格 | 计费单位 |
|---|---:|---|
| Seedream 5.0 Lite | ¥0.22 | 每张 |
| Seedream 5.0 Pro | ¥0.30 | 每张 |

Seed 2.0 Lite 等文本模型暂不纳入计价范围。

Seedance 系列按输出 Token 计费，价格单位为 `¥ / M output tokens`。同一模型会按输入是否包含视频、输出分辨率和实际路由服务商选择不同费率。

常规费率（截图，2026-08-28）：

| 模型 | 实际服务商 | 输入包含视频 | 输入不含视频 |
|---|---|---|---|
| Seedance 2.0 | 特工宇宙 | 4K ¥16；480p/720p ¥28；1080p ¥31 | 4K ¥26；480p/720p ¥46；1080p ¥51 |
| Seedance 2.0 Fast | 特工宇宙 | 480p/720p ¥22 | 480p/720p ¥37 |
| Seedance 2.0 Mini | 特工宇宙 | 480p/720p ¥14 | 480p/720p ¥23 |
| Seedance 2.5 | 火山方舟 | 1080p ¥33.12；480p/720p ¥42 | 1080p ¥55.44；480p/720p ¥70 |
| Seedance 2.5 | 特工宇宙 | 480p/720p ¥42 | 480p/720p ¥70 |

截图还包含火山方舟的限时折扣，需按有效期自动参与计价。当前按北京时间 `2026-09-17 14:00` 作为排他截止点：截止前使用促销价，截止时刻起恢复常规价。

| 模型 | 输入包含视频 | 输入不含视频 |
|---|---|---|
| Seedance 2.0 Fast | 480p/720p ¥16.5（原 ¥22） | 480p/720p ¥27.75（原 ¥37） |
| Seedance 2.0 Mini | 480p/720p ¥5.6（原 ¥14） | 480p/720p ¥9.2（原 ¥23） |

TokenDance 页面说明请求会自动路由并在故障时切换服务商。系统当前 Job 没有记录实际路由服务商，因此不能在提交前安全选择服务商费率，也不能默认取最低价。后续需通过真实调用探测 API 是否返回实际路由服务商与输出 Token；探测前保持不计价。

## 已知边界

- 费用是提交时的预计值，不代表厂商最终账单；失败任务也不能据此判断是否真实扣费。
- 旧 Job 默认不在运行时动态重算；已核实渠道可执行一次性、可备份的快照回填。
- 新增渠道或模型必须先核实自身价格与协议，再加入白名单和测试。
