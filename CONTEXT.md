# CONTEXT — 领域术语表

> 本文件只是术语表:统一说法,不写实现细节。实现契约见 `docs/api-contract.md`。

## 作品(Gallery Item)

一张可在画廊类界面(首页作品展示、项目页作品区)展示的成品图。来源有两类:**角色作品**(角色目录三个资产槽下的图)与 **Studio 作品**(Studio 自由出图的产物)。视频不算作品,不进画廊。

## 隐藏(Hidden)

对单张作品的标记,含义是**不在画廊类界面展示**。不是删除:原文件、出图记录、工坊内的展示都不受影响,且可随时取消。粒度永远是单图,不存在"整轮隐藏"。

## 轮(Round)

Studio 中一次出图提交产生的一组图(1-n 张)。删除按轮,隐藏按单图。

## 项目作品(Project Works)

一个项目名下全部角色的角色作品之和。角色与项目的归属关系由分配表(assignments)决定;未分配项目的角色不属于任何项目作品。Studio 作品无角色归属,因此永远不属于任何项目作品。

## 首页展示 Studio 作品(开关)

应用级设置,默认关闭。开启后 Studio 作品与角色作品在首页作品展示中混排;已隐藏的作品在两类来源下都被过滤。
