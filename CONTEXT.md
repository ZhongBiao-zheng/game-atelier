# CONTEXT — 领域术语表

> 本文件只是术语表:统一说法,不写实现细节。实现契约见 `docs/api-contract.md`。

## 项目组织

**项目(Project)**:
一款游戏及其长期资产的唯一归属边界。版本活动、营销节点和单项交付都不是项目。
_避免_:把美术、UI、视频分别建成项目。

**项目基线(Project Baseline)**:
项目内长期生效的世界观、视觉方向、UI 原则和品牌基调。项目文件夹的备注可以补充当前主题,但不能静默改写项目基线。
_避免_:全局风格文件、项目文件夹。

**工作区(Workspace)**:
从美术、UI 或视频专业视角查看和生产项目内容的固定入口。工作区是视图,不是资产归属容器。
_避免_:项目类型、媒体文件夹。

**资产库(Asset Library)**:
项目永久拥有的全部可复用资产集合,是每个项目的固定区域。同一资产只在资产库中存在一份,可出现在多个项目文件夹中。
_避免_:长期资产文件夹、公共文件夹。

**项目文件夹(Project Folder)**:
用户在单个项目内建立的个人整理视图,可混合收纳美术、UI 和视频资产。文件夹只引用资产,不拥有、复制或搬动资产。
_避免_:子项目、交付包、资产目录。

**文件夹成员(Folder Item)**:
项目文件夹对项目资产的一条引用。同一资产可以出现在多个文件夹中,从文件夹移除不等于删除资产。
_避免_:资产副本、文件移动。

## 生产对象

**资产(Asset)**:
由项目拥有、可独立迭代并可跨文件夹复用的生产对象。资产可以有自己的制作版本和定稿。
_避免_:附件、文件夹文件。

**角色变体(Character Variant)**:
从母角色继承身份锚点、同时拥有独立 Spec、完整美术流程和定稿的角色资产,例如角色皮肤。
_避免_:角色图片、皮肤文件夹、角色副本。

**UI 方案(UI Scheme)**:
一套并存的 UI 风格、组件和页面集合,例如 V1 与 V2。方案之间可以复制现有页面作为起点,之后各自保存版本,不建立复杂分支图。
_避免_:覆盖版、最终版。

项目级 GDD / PRD / 交互锚与视觉基线由所有 UI 方案共享；方案只独立拥有自己的 UI 风格说明、
页面地图、页面版本和定稿。默认方案只决定打开入口，不代表唯一或最终方案。

旧模型中的无方案 UI 在升级时整体成为 V1；升级后只存在 UI 方案模型，不与无方案 UI 长期并存。

**视频企划(Video Production)**:
围绕一个视频交付目标组织的 Brief、镜头表、镜头版本和成片集合。视频企划可以引用角色、UI 等项目资产的明确版本。
_避免_:视频文件夹、单条视频 Job。

## 版本与状态

**定稿(Canonical)**:
某个生产对象当前选中使用的制作版本。新的定稿不会改写历史 Job 已记录的参考路径。
_避免_:最新文件、最终最终版。

**过时(Stale)**:
对象所依赖的项目基线或母资产已变化,需要人工判断是否重做的状态。过时只发出提示,不自动覆盖下游产物。
_避免_:自动同步、自动重生成。

## 作品(Gallery Item)

一张可在画廊类界面(首页作品展示、项目页作品区)展示的成品图。来源有两类:**角色作品**(角色目录三个资产槽下的图)与 **Studio 作品**(Studio 自由出图的产物)。视频不算作品,不进画廊。

## 隐藏(Hidden)

对单张作品的标记,含义是**不在画廊类界面展示**。不是删除:原文件、出图记录、工坊内的展示都不受影响,且可随时取消。粒度永远是单图,不存在"整轮隐藏"。

## 轮(Round)

Studio 中一次出图提交产生的一组图(1-n 张)。删除按轮,隐藏按单图。

## 项目作品(Project Works)

一个项目所拥有角色及角色变体的角色作品之和。项目文件夹只引用这些资产,不改变作品的项目归属;Studio 作品无项目归属,因此不属于任何项目作品。

## 首页展示 Studio 作品(开关)

应用级设置,默认关闭。开启后 Studio 作品与角色作品在首页作品展示中混排;已隐藏的作品在两类来源下都被过滤。
