# CONTEXT — 领域术语表

> 本文件只是术语表:统一说法,不写实现细节。实现契约见 `docs/api-contract.md`。

## 项目组织

**项目(Project)**:
一款游戏及其长期资产的唯一归属边界。版本活动、营销节点和单项交付都不是项目。
_避免_:把美术、UI、视频分别建成项目。

**项目索引(Project Index)**:
集中浏览、创建和管理全部项目的入口。它以项目卡片墙作为主入口,不保留重复的项目列表侧栏,也不承载任何单个项目的策划或资产生产内容。每张项目卡片只展示项目封面、完整项目名、项目活动时间与项目管理菜单;新建项目是卡片墙中的首个入口。
_避免_:项目首页、项目列表侧栏、资产库。

**项目首页(Project Home)**:
单个项目的阅读优先概览,由项目经验、世界观与项目画廊共同构成。项目经验与世界观默认只读,由显式“编辑”动作进入编辑态。项目画廊中的作品先进入统一媒体预览,再由预览进入对应资产详情。项目首页用于理解和回顾项目,不承担具体资产的生产管理。
_避免_:项目索引、资产库、工作区总览。

**项目切换器(Project Switcher)**:
进入单个项目后位于侧栏顶部的项目上下文切换入口。切换项目时统一落到目标项目首页,不沿用来源项目的资产或工作区深层路径。
_避免_:项目索引卡片墙、跨项目深层路由映射。

**项目导航(Project Navigation)**:
项目切换器下方、只服务当前项目的固定导航,用于进入项目首页以及角色、UI、视频资产区域。
_避免_:全局项目列表、项目首页内的重复工作区标签栏。

**项目封面(Project Cover)**:
项目在索引中的视觉识别摘要,由该项目最新四张未隐藏图片作品媒体自动组成。视频作品媒体不参与项目封面。
_避免_:手动封面资产、最新视频、项目 Logo。

**项目活动时间(Project Activity Time)**:
项目任意内容最近一次变化的时间,包括项目基线、策划内容和资产产出,而不只是最新作品媒体的时间。
_避免_:创建时间、最新出图时间。

**项目基线(Project Baseline)**:
项目内长期生效的世界观、视觉方向、UI 原则和品牌基调。
_避免_:全局风格文件、临时创作说明。

**工作区(Workspace)**:
从美术、UI 或视频专业视角查看和生产项目内容的固定入口。工作区是视图,不是资产归属容器。
_避免_:项目类型、媒体文件夹。

**资产库(Asset Library)**:
项目永久拥有的全部可复用资产集合,是每个项目的固定区域。角色、UI 和视频资产直接按自身类型进入对应入口,不再增加额外整理层。
_避免_:长期资产文件夹、公共文件夹、项目文件夹。

**角色索引(Character Index)**:
当前项目内集中浏览和进入全部角色的入口。角色索引只组织角色资产,不拥有角色作品或跨项目聚合内容。
_避免_:角色列表侧栏、角色项目。

**最近角色(Recent Characters)**:
当前项目中按用户最近打开时间排序的角色快捷集合,最多展示五个。它只服务导航,不表示角色的重要性或最近产出时间。
_避免_:最新角色、活跃角色、最近更新角色。

**角色工作台(Character Workspace)**:
单个角色的生产管理视图,集中呈现该角色自己的美术资产和与其明确关联的项目 UI、视频作品。角色工作台不成为新的资产归属边界。
_避免_:角色项目、角色文件夹。

## 本机工坊接入

以下术语用于[开发中的网站与 MCP 方案](docs/local-workspace.md)，不表示功能已上线。

**本机工坊(Local Atelier)**:
由用户电脑上的 Atelier 服务与数据目录共同承载的创作环境，网站和 Agent 只是访问入口，不拥有其项目数据。
_避免_:云端工作区、浏览器缓存库、远程桌面。

**工坊工具会话(Workshop Tool Session)**:
外部 Agent 在用户批准的项目与操作范围内访问工坊的一次连接身份，不等同于画布 Agent 会话或文件系统权限。
_避免_:全局 Agent 授权、Canvas Approval、已安装 Skill。

**工坊生成请求(Workshop Generation Request)**:
用户批准前冻结的目标、提示词、参考素材和模型参数，以及对这份内容的批准记录；实际执行结果仍由 Job 拥有。
_避免_:Job 副本、自动工作流、聊天里的同意文字。

## 生产对象

**资产(Asset)**:
由项目拥有、可独立迭代的生产对象。资产可以有自己的制作版本和定稿。
_避免_:附件、整理关系。

**角色衍生(Character Derivative)**:
以一个已有角色或一组内容图片作为创作来源建立的独立角色资产。来源只用于初始化和追溯,衍生角色与其他角色平级,拥有自己的 Spec、立绘、美宣、三视图、版本和定稿。
_避免_:皮肤、母子角色、角色变体、嵌套角色。

**UI 方案(UI Scheme)**:
一套并存的 UI 风格、组件和页面集合,例如 V1 与 V2。方案之间可以复制现有页面作为起点,之后各自保存版本,不建立复杂分支图。
_避免_:覆盖版、最终版。

项目级 GDD / PRD / 交互锚与视觉基线由所有 UI 方案共享；方案只独立拥有自己的 UI 风格说明、
页面地图、页面版本和定稿。默认方案只决定打开入口，不代表唯一或最终方案。

旧模型中的无方案 UI 在升级时整体成为 V1；升级后只存在 UI 方案模型，不与无方案 UI 长期并存。

**视频企划(Video Production)**:
围绕一个视频交付目标组织的 Brief、镜头表、镜头版本和成片集合。视频企划可以引用角色、UI 等项目资产的明确版本。
_避免_:单条视频 Job、视频页面。

**角色关联作品(Character-related Media)**:
仍由项目拥有、但与某个角色建立明确关联的 UI 或视频作品媒体。关联可以由生产中真实使用角色资产形成,也可以由用户显式补充;它不复制媒体,也不改变资产归属。
_避免_:角色所有的 UI、角色所有的视频、角色作品副本。

**角色关联(Character Association)**:
角色与 UI 页面或视频企划之间的多对多关系,来源可以是已记录的真实生产引用或用户显式指定。关联作用于生产对象而非单个媒体版本;删除关联只改变角色工作台中的聚合结果,不删除作品或生产对象。
_避免_:角色归属、作品搬入角色、角色标签。

## 创作资产

**创作资产(Creation Asset)**:
由应用级个人创作资产库拥有、可在创作台与全部画布项目中复用的内容对象。它拥有唯一身份和版本历史,画布项目只建立收藏或使用关系,不创建另一份可独立编辑的资产。
_避免_:创作台历史记录、画布本地副本、工坊正式资产。

**提示词资产(Prompt Asset)**:
以可复用提示词正文为内容的创作资产,可以包含需要在使用时填写的可选变量。使用提示词资产只填入最终提示词正文,不恢复模型、生成参数或参考素材。
_避免_:生成配方、生成快照、本地提示词。

**提示词变量(Prompt Variable)**:
提示词资产正文中带名称和默认内容的可替换片段。同名变量在一次使用中共享填写内容;用户不填写时使用默认内容。使用时显示的灰色变量名称只提示这一段适合填写什么,不进入最终提示词正文;取消变量身份会保留当前文字并与前后文无空格拼接。
_避免_:填写说明、示例占位、模型参数。

**图片资产(Image Asset)**:
以一张可复用图片为内容的创作资产。一次批量保存会创建多个独立图片资产;图片资产可以在创作台和画布中作为参考素材使用,但不等同于一次生成记录或工坊正式资产。
_避免_:图片 Job、作品媒体、参考图副本。

**创作资产版本(Creation Asset Version)**:
创作资产内容的一次不可变快照。修改提示词正文或变量、替换图片会创建新版本;修改标题或标签只更新资产元数据。新的使用默认选择最新版本,已经插入或提交的内容保持原版本,只有用户明确更新时才切换;恢复旧内容会把它复制成新的最新版本,不改写版本顺序。
_避免_:覆盖旧内容、实时改写生成历史。

**创作资产使用(Creation Asset Use)**:
用户把创作资产明确应用到当前创作上下文的动作。在打开的生成草稿中,提示词资产填入提示词正文、图片资产成为参考素材;没有打开生成草稿时,画布把它们分别创建为文本节点或图片节点。使用不会修改资产模板;用户修改已填入的提示词后,该草稿立即解除资产关联并成为普通提示词。未修改的关联内容明确更新到新版本时,同名变量保留本次填写值,新增变量使用新默认值,已删除变量随版本移除。
_避免_:自动填充、静默替换、复制资产身份。

**创作资产归档(Creation Asset Archive)**:
用户把创作资产从日常列表中移出的可恢复状态。归档不删除资产身份、版本、画布关系或历史引用;第一版不提供物理删除。
_避免_:永久删除、移出项目、隐藏作品媒体。

## 创作画布

**画布项目(Canvas Project)**:
由用户在“画布”顶级功能区中手动新建的独立创作空间,拥有自己的画布文档、上传资源与画布 Job。它不属于创作台或工坊项目,也不由 Character/UI/Video Skill 创建、初始化或推进。
_避免_:工坊项目、Skill 项目、创作台视图。

**创作画布(Creative Canvas)**:
画布项目内部用于人工添加、摆放、关联和继续创作内容的空间编辑器。每个画布项目有且只有一张创作画布;创作画布不负责执行整张工作流。
_避免_:创作台、工坊工作区、工作流引擎。

**画布节点(Canvas Node)**:
用户在创作画布中手动创建的稳定交互对象,可以承载内容、生成配置、批量素材、分组或插件能力。节点拥有画布内的位置与呈现状态,但不拥有生成历史真实性。
_避免_:Skill 步骤、工坊资产、自动生成流程、Job 记录。

**内容节点(Content Node)**:
承载当前文本或媒体内容版本的稳定画布节点。空内容与已有内容是同一节点的不同状态,不是不同节点类型。
_避免_:生成节点、资源附件、Job 节点。

**图层栈节点(Layer Stack Node)**:
承载一次图片图层拆分结果的稳定画布节点。它以一张背景图和按层级排列的透明 PNG 重建原图,允许用户独立显隐和下载各图层;图层位置与名称来自提交时冻结的拆分结果。
_避免_:普通图片节点、浏览器临时叠图、可执行工作流节点。

**生成配置节点(Generation Config Node)**:
只承载可编辑生成草稿与输入候选的画布节点,不拥有生成结果。
_避免_:结果节点、工作流步骤、模型任务。

**输入连接(Input Connection)**:
从内容提供者指向生成表面的当前可编辑输入资格关系。它不代表输入已经实际使用,也不会触发目标节点或整图执行。
_避免_:来源记录、执行连接、数据依赖。

**派生连接(Derivation Connection)**:
一次生成或确定性本地工具操作与结果内容节点之间的可视关系。它用于画布叙事,不是历史真源,删除后也不改写生成快照或内容版本来源。
_避免_:来源真源、执行连接、工作流边。

**生成草稿(Generation Draft)**:
生成表面上尚未提交、可以继续编辑的创作意图,包含提示词、模型、参数与输入选择策略。
_避免_:生成记录、Job 快照、最终参数。

**解析输入(Resolved Input)**:
提交前由生成草稿、当前输入连接和显式引用共同解析出的临时输入集合。它尚未成为历史记录。
_避免_:输入连接、生成快照、节点当前内容。

**生成快照(Generation Snapshot)**:
提交时冻结的实际提示词、模型、参数与具体内容版本,是回答“一次生成真实使用了什么”的唯一真源。
_避免_:输入连接、节点当前状态、可编辑草稿。

**画布生成轮(Canvas Generation Run)**:
一次明确提交及其 Canvas Job、生成快照、候选结果和最终状态的集合。
_避免_:整图运行、工作流执行、单个结果文件。

**画布资产条目(Canvas Library Entry)**:
画布项目对一个应用级创作资产的收藏或使用关系。资产首次在画布中使用时自动加入本项目范围;移出只删除关系,不删除全局资产、已有节点或生成历史。
_避免_:画布本地副本、资产所有权、内容版本副本。

**画布转入(Canvas Transfer)**:
用户把另一个画布、创作台或工坊中的内容显式加入目标画布项目的动作。已登记的创作资产会建立项目关系并固定本次使用的资产版本;未登记内容通过“保存并加入画布”先显式保存为创作资产,再建立关系。目标画布中的既有内容不会被新版本静默替换。
_避免_:自动保存、实时覆盖、移动资产。

**画布发布(Canvas Publication)**:
用户把画布内容显式复制为某个兼容工坊目标所拥有的新版本。发布不移动画布内容,也不建立后续同步关系。
_避免_:归属变更、共享文件、自动归档。

**画布项目包(Canvas Project Package)**:
一个或多个画布项目及其自有内容和历史的可移植快照。导入项目包会创建新项目,且包不携带凭证或已安装代码。
_避免_:项目复制命令、设置备份、裸媒体压缩包。

**同步快照(Sync Snapshot)**:
一个画布项目在同步谱系中的不可变可恢复状态。同步只推进已确认的谱系,不按修改时间合并分叉内容。
_避免_:实时协作状态、字段级合并、最近修改覆盖。

**同步冲突副本(Sync Conflict Copy)**:
当本地与远端同步快照发生分叉时,为完整保留其中一个分支而创建的新画布项目。
_避免_:冲突合并、覆盖版、临时备份。

**人工画布创作(Human-led Canvas Creation)**:
画布项目的创建和每一轮协作都由用户主动发起,不存在自行启动的工作流或后台推进。它允许用户批准助手提出的项目内修改,不要求每个变化都由用户亲手拖拽完成。
_避免_:自动工作流、后台 Agent、自运行画布。

**工作流 Skill(Workflow Skill)**:
用于 Character、UI 或 Video 等正式生产流程的 Skill。它不创建、填充或推进画布项目。
_避免_:Canvas Agent Skill、画布自动化。

**画布 Agent(Canvas Agent)**:
用户在一个画布项目内主动发起的受限协作助手,可以读取项目并提出获授权的修改,但不能自行推进项目或管理项目生命周期。
_避免_:Workflow Skill、后台执行器、文件系统 Agent。

**画布 Agent Skill(Canvas Agent Skill)**:
用户为下一次 Agent 交互轮选择的指令模板。它不能独立执行,也不能扩大 Agent 的工具或批准权限。
_避免_:Workflow Skill、可执行插件、后台任务。

**Agent 交互轮(Agent Turn)**:
用户在一个画布项目会话中主动发起的一次请求、响应与提案生命周期。
_避免_:后台计划、跨项目任务、自动续跑。

**画布变更集(Canvas Change Set)**:
Canvas Agent 提出的一组原子、可验证、可审计的画布修改。它只有在适用的画布批准下才能提交。
_避免_:任意 JSON Patch、浏览器状态覆盖、Job 执行。

**画布批准(Canvas Approval)**:
用户对特定项目、变更集或当前会话内操作类别授予的许可。它不等同于文件、凭证、网络或项目生命周期权限。
_避免_:全局授权、danger-full-access、后台同意。

**画布节点插件(Canvas Node Plugin)**:
为创作画布增加节点呈现和受控能力的可安装扩展。它不拥有画布项目数据,也不成为工作流或模型执行入口。
_避免_:Workflow Skill、Caller Adapter、宿主页脚本。

**画布插件包(Canvas Plugin Package)**:
以内容摘要固定版本的插件代码与资源集合。它属于应用安装环境,不属于画布项目,也不随画布项目包携带。
_避免_:画布项目包、远程脚本 URL、插件状态。

**画布插件批准(Canvas Plugin Grant)**:
用户允许某个插件包摘要在特定画布项目内使用明确能力的许可。它不跨项目或插件版本,也不授予文件、凭证或任意网络权限。
_避免_:安装同意、全局权限、通配授权。

**插件节点载荷(Plugin Node Payload)**:
由画布项目拥有、按插件节点 schema 保存的 JSON 内容。插件缺失或禁用时它仍被原样保留。
_避免_:插件代码、插件缓存、Job 状态。

**调用配置(Caller Profile)**:
把模型协议声明为受控请求与响应映射的无代码配置。它由 Job Runner 使用,不是可执行插件或浏览器脚本。
_避免_:Caller Adapter、任意调用脚本、节点插件。

**分组节点(Group Node)**:
组织一层画布空间与显式成员关系的容器。用户可以主动执行分组内的生成链路;分组本身不提供内容、不成为连线端点,也不改变资产归属。解散只移除分组,保留成员。
_避免_:子画布、自动工作流、资产文件夹。

**批量素材(Batch Material)**:
用户明确整理的一组有序素材项,默认每张图片独立一项,也可给同项追加参考图。提供给生成节点的是当前项的素材,不是把整批图片同时当成参考。
_避免_:生成节点、候选结果、自动资产入库。

**批量执行(Batch Execution)**:

画布按项与轮展开普通产物节点链路，每批一排；图片接到同批视频，不共用一个结果节点。
第一批复用原配置链路，其余批次的产物保留独立位置与连接。视频结果直接显示为节点，不使用候选堆叠。
用户确认的一次多项或重复生成计划。单节点执行不要求分组;多节点链路以显式分组限定范围,同项的上游结果仅传给同项下游。提示词、素材和设置在确认前冻结,连线本身仍不触发生成。停止不代表撤回已向厂商提交的请求。
_避免_:整图自动运行、单个 Job、候选数量。

**重复轮数(Repeat Count)**:
同一批素材或固定输入完整执行多少轮。每项每步的产物数量与轮数是不同概念。
_避免_:参考图数量、候选数量、并发数。

## 版本与状态

**定稿(Canonical)**:
某个生产对象当前选中使用的制作版本。新的定稿不会改写历史 Job 已记录的参考路径。
_避免_:最新文件、最终最终版。

**过时(Stale)**:
对象所依赖的项目基线或母资产已变化,需要人工判断是否重做的状态。过时只发出提示,不自动覆盖下游产物。
_避免_:自动同步、自动重生成。

## 画廊呈现

**作品媒体(Gallery Media)**:
资产生产流程成功产出或由用户导入到成品版本位置的可视媒体文件,类型可以是图片或视频。美术图、UI 页面图和视频产物的全部历史版本都可以成为作品媒体;参考图、源图、上传素材、失败记录、世界观、Spec、Brief、Prompt、镜头表等不属于作品媒体。
_避免_:资产、策划内容、Job 记录。

**项目画廊(Project Gallery)**:
跨美术、UI 和视频聚合展示项目全部未隐藏作品媒体的阅读优先视图,按产出时间倒序排列并渐进加载。默认混合展示,可按美术、UI、视频过滤;筛选只改变展示,不改变资产归属。项目画廊不拥有、复制或编辑资产,也不代替资产库。
_避免_:项目作品、资产库、出图历史。

**隐藏(Hidden)**:
对单个作品媒体的标记,含义是**不在画廊类界面展示**。不是删除:原文件、出图记录和资产库内的展示都不受影响,且可随时取消。

## 轮(Round)

Studio 中一次出图提交产生的一组图(1-n 张)。删除按轮,隐藏按单图。

## 首页展示 Studio 作品(开关)

应用级设置,默认关闭。开启后 Studio 图片作品与角色图片作品在应用首页混排;已隐藏的作品媒体在两类来源下都被过滤。
