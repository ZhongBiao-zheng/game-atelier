# CONTEXT — 领域术语表

> 本文件只是术语表:统一说法,不写实现细节。实现契约见 `docs/api-contract.md`。

## 项目组织

**项目(Project)**:
一款游戏及其长期资产的唯一归属边界。版本活动、营销节点和单项交付都不是项目。
_避免_:把美术、UI、视频分别建成项目。

**项目索引(Project Index)**:
集中浏览、创建和管理全部项目的入口。它以项目卡片墙作为主入口,不保留重复的项目列表侧栏,也不承载任何单个项目的策划或资产生产内容。每张项目卡片只展示项目封面、完整项目名、项目活动时间与项目管理菜单;新建项目是卡片墙中的首个入口。
_避免_:项目首页、项目列表侧栏、资产库。

**项目首页(Project Home)**:
单个项目的阅读优先概览,由项目经验、世界观与项目画廊共同构成。项目经验与世界观默认只读,由显式“编辑”动作进入编辑态。项目画廊中的作品先进入统一媒体预览,再由预览进入对应资产详情。项目首页用于理解和回顾项目,不承担具体资产的生产管理。
_避免_:项目索引、资产库、工作区总览。

**项目切换器(Project Switcher)**:
进入单个项目后位于侧栏顶部的项目上下文切换入口。切换项目时统一落到目标项目首页,不沿用来源项目的资产或工作区深层路径。
_避免_:项目索引卡片墙、跨项目深层路由映射。

**项目导航(Project Navigation)**:
项目切换器下方、只服务当前项目的固定导航,用于进入项目首页以及角色、UI、视频资产区域。
_避免_:全局项目列表、项目首页内的重复工作区标签栏。

**项目封面(Project Cover)**:
项目在索引中的视觉识别摘要,由该项目最新四张未隐藏图片作品媒体自动组成。视频作品媒体不参与项目封面。
_避免_:手动封面资产、最新视频、项目 Logo。

**项目活动时间(Project Activity Time)**:
项目任意内容最近一次变化的时间,包括项目基线、策划内容和资产产出,而不只是最新作品媒体的时间。
_避免_:创建时间、最新出图时间。

**项目基线(Project Baseline)**:
项目内长期生效的世界观、视觉方向、UI 原则和品牌基调。
_避免_:全局风格文件、临时创作说明。

**工作区(Workspace)**:
从美术、UI 或视频专业视角查看和生产项目内容的固定入口。工作区是视图,不是资产归属容器。
_避免_:项目类型、媒体文件夹。

**资产库(Asset Library)**:
项目永久拥有的全部可复用资产集合,是每个项目的固定区域。角色、UI 和视频资产直接按自身类型进入对应入口,不再增加额外整理层。
_避免_:长期资产文件夹、公共文件夹、项目文件夹。

## 生产对象

**资产(Asset)**:
由项目拥有、可独立迭代的生产对象。资产可以有自己的制作版本和定稿。
_避免_:附件、整理关系。

**角色衍生(Character Derivative)**:
以一个已有角色或一组内容图片作为创作来源建立的独立角色资产。来源只用于初始化和追溯,衍生角色与其他角色平级,拥有自己的 Spec、立绘、美宣、三视图、版本和定稿。
_避免_:皮肤、母子角色、角色变体、嵌套角色。

**UI 方案(UI Scheme)**:
一套并存的 UI 风格、组件和页面集合,例如 V1 与 V2。方案之间可以复制现有页面作为起点,之后各自保存版本,不建立复杂分支图。
_避免_:覆盖版、最终版。

项目级 GDD / PRD / 交互锚与视觉基线由所有 UI 方案共享；方案只独立拥有自己的 UI 风格说明、
页面地图、页面版本和定稿。默认方案只决定打开入口，不代表唯一或最终方案。

旧模型中的无方案 UI 在升级时整体成为 V1；升级后只存在 UI 方案模型，不与无方案 UI 长期并存。

**视频企划(Video Production)**:
围绕一个视频交付目标组织的 Brief、镜头表、镜头版本和成片集合。视频企划可以引用角色、UI 等项目资产的明确版本。
_避免_:单条视频 Job、视频页面。

## 版本与状态

**定稿(Canonical)**:
某个生产对象当前选中使用的制作版本。新的定稿不会改写历史 Job 已记录的参考路径。
_避免_:最新文件、最终最终版。

**过时(Stale)**:
对象所依赖的项目基线或母资产已变化,需要人工判断是否重做的状态。过时只发出提示,不自动覆盖下游产物。
_避免_:自动同步、自动重生成。

## 画廊呈现

**作品媒体(Gallery Media)**:
资产生产流程成功产出或由用户导入到成品版本位置的可视媒体文件,类型可以是图片或视频。美术图、UI 页面图和视频产物的全部历史版本都可以成为作品媒体;参考图、源图、上传素材、失败记录、世界观、Spec、Brief、Prompt、镜头表等不属于作品媒体。
_避免_:资产、策划内容、Job 记录。

**项目画廊(Project Gallery)**:
跨美术、UI 和视频聚合展示项目全部未隐藏作品媒体的阅读优先视图,按产出时间倒序排列并渐进加载。默认混合展示,可按美术、UI、视频过滤;筛选只改变展示,不改变资产归属。项目画廊不拥有、复制或编辑资产,也不代替资产库。
_避免_:项目作品、资产库、出图历史。

**隐藏(Hidden)**:
对单个作品媒体的标记,含义是**不在画廊类界面展示**。不是删除:原文件、出图记录和资产库内的展示都不受影响,且可随时取消。

## 轮(Round)

Studio 中一次出图提交产生的一组图(1-n 张)。删除按轮,隐藏按单图。

## 首页展示 Studio 作品(开关)

应用级设置,默认关闭。开启后 Studio 图片作品与角色图片作品在应用首页混排;已隐藏的作品媒体在两类来源下都被过滤。
