# 游戏角色资产工作流·产品形态与交互层 设计方案 v2.2

| 字段 | 内容 |
|---|---|
| **给谁看** | 项目负责人本人（决策定盘）+ 美术负责人（接受度评估）+ 后续实施者 |
| **文档定位** | 产品形态与交互层的最终设计 v2.1，承接功能说明书 v0.2 的"§产品形态与交互层"待定项 |
| **承接文档** | [2026-05-14 游戏角色资产工作流-功能说明书.md](./2026-05-14%20游戏角色资产工作流-功能说明书.md) v1.0 |
| **取代文档** | [2026-05-14 游戏角色资产工作流-产品形态设计方案.md](./2026-05-14%20游戏角色资产工作流-产品形态设计方案.md) v1.0（保留作历史归档）|
| **修订触发** | (1) 2026-05-15 office-hours 会话暴露 v1.0 三个前提偏差 → v2.0；(2) 2026-05-15 CEO Review 三轮迭代（回填指标 / 并发写约定 / 图片元信息面板 / SSE 架构决策 / clipboard 日志）→ v2.2 |
| **生成日期** | 2026-05-15 |
| **状态** | 设计方案 v2.2，已通过 office-hours 诊断 + CEO Review + Outside Voice 挑战，**待写入实施计划** |
| **下一步** | 进入 writing-plans 阶段，拆解 **B+ 阶段（单阶段，2 周）** 实施计划 |

**v2.0 → v2.2 变更摘要**：
- §4.2 新增交付物 9：图片元信息面板（每张图都自带 prompt/time/model/params/seed，可编辑可复制；替代 server.log 作为 M3 可观测性来源）
- §4.2 交付物 2：文件监听升级为 SSE 推送（`GET /events`），替代纯 polling 方案
- §4.5：移除"SSE/WebSocket"不做项（SSE 现在是已选架构）；新增"WebSocket（双向持久）不做"
- §4.6 验收指标从 TBD 回填为 4 个可测项；M2 增加 clipboard 成功率的测量机制
- §5 新增 §5.4：Skill ↔ Web 并发写约定（非破坏式同步，以本地落盘为准）
- §9 新增 3 项 TODO（Web 内独立出图板块 → V2；图片元信息字段定义 → 实施期间；plan-design-review → writing-plans 前）

---

## 0. 一句话决定

**Skill 主交互 + 本地 Web UI 双向 + 剪贴板桥接，单阶段 2 周一步到位。**

- Skill 跑在画师自己的 Claude Code 里（用画师自己的 Claude 账号）
- 本地 Web UI 是 Skill 启动的伴生窗口，localhost，无服务器、无云、无账号
- 画师**主体在 Web 工作**：看图、编辑 prompt、改角色档案、提反馈
- 画师"触发下一轮"用 **1.5 次操作**：在 Web 点保存后，回 CC 按 `Cmd+V` + `Enter`（"继续"已自动复制到剪贴板）
- 文件系统是唯一 source of truth；图片不进 Git
- **取消 v1.0 的两阶段路径**（B 只读看图 → C Web 主交互）

---

## 1. v1.0 → v2.0 关键变更摘要

| 维度 | v1.0 | v2.0 |
|---|---|---|
| 路径 | B 阶段（2-3 天 只读看图）→ C 阶段（2-3 周 Web 主交互） | **B+ 单阶段**（2 周 Web 双向） |
| Web 角色 | B 是只读、C 才升主 | 一上来就是双向（看图 + 编辑 + 反馈）|
| 画师入口主体 | B 阶段终端为主 / C 阶段 Web 为主 | **画师主体在 Web，CC 终端只是触发回合** |
| Web → Skill 通信 | C 阶段：inbox 队列 + "回 CC 输继续" | **剪贴板桥接：Cmd+V + Enter** |
| 总工期 | 3-4 周 | **2 周** |
| inbox 队列复杂度 | 完整文件队列 + lock + 时间戳排序 | **取消** —— 改为 Web 直接写文件、剪贴板触发"继续" |

---

## 2. 前提修订记录（office-hours 2026-05-15 会话）

v1.0 隐含的三个前提，会话中被推到台面验证。结果：

| v1.0 前提 | 验证后状态 | 修订方向 |
|---|---|---|
| **画师不愿在 CC 终端打字、心智门槛高** | ❌ 不成立 | 画师当前**已经在跟 LLM 对话**（喂资料、改 prompt、反复出图）。"不愿打字"是创业者投影，不是事实 |
| **真痛点是"画师对终端不熟"** | ❌ 不成立 | 真痛点是**"重复喂上下文"**——每接一个新角色都要重新喂项目资料/已有形象/风格基线给 LLM。Skill 直接解药 |
| **C 阶段必须"Web 主交互"为画师不愿打字买保险** | ⚠️ 部分修订 | 画师**确实必须图形界面**（终端看不了图、编辑长 prompt 反人类），但"图形界面"≠"Web 退到后端"。B+ 在保留 Skill 主导的同时让画师全程在 Web |

**保留不动的原则**（v1.0 §9 哲学锚仍然有效）：

1. 文件系统是唯一 source of truth
2. Skill 内核纯 markdown + tool calls，不绑死任何 agent host
3. 画师写动作的最终归宿是文件
4. 画师本地有完整权力（PS / Finder / VS Code 绕过 UI 直改）
5. 零部署、零账号、零云

---

## 3. 候选方案再评估（含 B+）

| 维度 | A. 纯 Skill | B. v1.0 B 阶段（只读 Web） | **B+. 本方案** | C. v1.0 C 阶段（Web 主+回合制） | D. 桌面 GUI App |
|---|---|---|---|---|---|
| 画师主入口 | CC 终端 | CC 终端 + 浏览器看图 | **浏览器（看 + 编 + 反馈）+ CC 终端（触发回合）** | 浏览器 | 桌面 App |
| 看图怎么解决 | Finder / PS | Web 渲染 | **Web 渲染** | Web 渲染 | App 内置 |
| 改 prompt 在哪 | CC 聊天 | CC 聊天 | **Web 表单** | Web 表单 + 聊天 | App 表单 |
| 改角色档案在哪 | CC 聊天 | CC 聊天 | **Web 表单** | Web 表单 | App 表单 |
| 触发下一轮 | CC 输入 | CC 输入 | **Cmd+V + Enter（剪贴板已就绪）** | 切窗口 + 打字"继续" | App 内按钮 |
| Web → Skill 通信 | 无 | 无 | **文件落盘 + 剪贴板** | inbox 队列 + 切窗口 | App 内 IPC |
| Skill 可移植性 | ✅ | ✅ | **✅ 仍可移植** | ✅ | ❌ Tauri 绑死 |
| 画师心智门槛 | 高 | 中 | **低** | 低 | 最低 |
| 工期（模型执行单位）| 0 | 2-3 天 | **2 周** | 3-4 周（含 B）| 3-4 周 |
| 服务器 / 账号 | 无 | 无 | **无** | 无 | 无 |
| 哲学锚（不绑死 host）| ✅ | ✅ | **✅** | ✅ | ❌ |
| 未来 SaaS 演化 | 要重写 UI | 要重写主交互 | **天然支持**（前后端分离的 localhost 版） | 天然支持 | 桌面 App 推倒 |

**为什么选 B+**：

1. **A、D 仍按 v1.0 理由淘汰**（A 看不了图、D 绑死 host）
2. **B 路径过于保守**：只读看图无法验证"画师能不能在 Web 完整工作"这个 v2.0 核心问题
3. **C 路径多余**：v1.0 把 C 当成"Web 主交互的最终形态"，但 C 设计了一套 inbox 队列 + "回 CC 输继续" 的回合制摩擦——这些是为了**强行让 Web 反向触发 Skill** 而引入的复杂度。B+ 用剪贴板桥接 1.5 次操作绕开
4. **B+ 同时拿到了 v1.0 的两个目标**：Web 完整 GUI（"以人为本"）+ Skill 内核可移植（架构哲学锚）
5. **B+ 工期最短**（2 周 vs A 路径的 3-4 周），且没有"B 阶段画师感受不到完整产品"的验证盲区

---

## 4. B+ 阶段范围（取代 v1.0 §3 + §4）

> **关于技术栈**：本设计仍是 stack-agnostic。Web Server 用 Node 还是 Python、前端用 React 还是 Vue——留给 writing-plans 阶段定盘。

### 4.1 整体架构

```
┌──────────────────────────────────────────────┐
│ 画师本人电脑（PC 或 Mac）                      │
│                                              │
│  ┌──────────────────┐  ┌──────────────────┐  │
│  │ Claude Code      │  │ 浏览器（主体）    │  │
│  │ （画师自己账号）  │  │ localhost:5173   │  │
│  │                  │  │                  │  │
│  │  character-      │  │  Web UI          │  │
│  │  workflow skill  │  │  - 图廊看图       │  │
│  │                  │  │  - 编辑 prompt   │  │
│  │  剪贴板写入"继续" │←─│  - 编辑角色档案  │  │
│  │  画师 Cmd+V +    │  │  - 反馈输入框    │  │
│  │  Enter 触发回合  │  │                  │  │
│  └──────────────────┘  └──────────────────┘  │
│        │     ↑              ↑     │          │
│        ▼     │              │     ▼          │
│  ┌──────────────────────────────────────┐   │
│  │ 本地文件系统（source of truth）       │   │
│  │  + characters/  + memory/            │   │
│  │  + .runtime/jobs/  + .runtime/draft/ │   │
│  └──────────────────────────────────────┘   │
└──────────────────────────────────────────────┘

【回合制】Web 改 → 文件落盘 + 剪贴板复制"继续"
        → 画师在 CC 按 Cmd+V + Enter → Skill 继续
```

**关键约束**：

- 文件系统是唯一数据源
- 画师**主体感受是在 Web 工作**，CC 终端只是"按个键触发下一轮"
- Skill 内核**零修改**，仍以"读文件、调工具、写文件"工作
- Web Server 不调任何 Claude API、不持有任何 token

### 4.2 交付物清单

| # | 组件 | 内容 |
|---|---|---|
| 1 | **localhost web server** | 单文件 Node script（或 Python FastAPI），默认端口 5173 |
| 2 | **文件监听 + SSE 推送**（v2.2 更新）| 监听 `<image_storage_root>/` 新文件、`.runtime/jobs/*.json` 状态变更；通过 **SSE**（`GET /events`）将文件变化实时推送给 Web 前端（替代纯 setInterval 轮询），实现"Content updated [刷新]"提示延迟 ≤500ms |
| 3 | **API 端点（读 + 写）** | `GET /jobs` / `GET /images?character=<id>` / `GET /spec/<id>` / **`POST /spec/<id>`** / **`POST /prompt/<job_id>`** / **`POST /feedback`** |
| 4 | **前端单页** | 三栏：左 = 任务进度 + 角色列表，中 = 图廊缩略图 + 大图，右 = 当前选中 prompt/档案的**可编辑表单** + 反馈输入框 |
| 5 | **剪贴板桥接** | 任何 POST 写完文件后：(a) 浏览器 Clipboard API 自动复制"继续"；(b) 顶部 toast 提示"已保存，回 CC 按 Cmd+V Enter" |
| 6 | **首次启动配置页** | 让画师选 / 粘贴图片存储目录，写入 `.runtime/config.json` |
| 7 | **Skill 端集成** | 新增 mini-skill `viewer-server`：`start` / `stop` / `open-browser`；image-gen 在出图后写一条 `jobs/*.json`；Skill 主循环每次 turn 起始检查 `.runtime/draft/` 增量 |
| 8 | **画师指南** | `docs/画师上手指南.md` 写完整流程 + 截图 + "Cmd+V + Enter" 触发动作的强调说明 |
| 9 | **图片元信息面板**（v2.1 新增）| 每次出图后 Skill 写 `.runtime/jobs/<job_id>.json` 含 `prompt / submitted_at / model / params / seed / output_path / status`；Web 点缩略图后右侧面板**完整展示这些字段，可编辑可复制**（复制即触发剪贴板"继续 + 改后的 prompt"，画师切 CC 粘贴可立即触发重出图）。同时承担"事后查阅 + 调试 + bug 复现"三重职责（替代传统 server.log）|

### 4.3 画师 B+ 阶段实际操作

```
画师（CC 终端）：/character-workflow 暗影刺客女
Skill：[启动 viewer-server → 弹浏览器 → 问几个问题 → 生成 spec.md → 推 Web]
画师：[切到浏览器，看 Web 上的 spec 草稿]
       [Web 表单里改"年龄 22 → 24"，点保存]
Web：[POST /spec → 写回 characters/shadow_spear_warrior.md]
     [剪贴板自动复制"继续"]
     [toast: 已保存，回 CC 按 Cmd+V Enter]
画师（回 CC）：[Cmd+V + Enter]
Skill：[读最新 spec.md → 生成 prompt → 推 Web → turn 结束]
画师：[Web 上看 prompt 表单，觉得矛太花，直接改"长矛"为"短刀"，点保存]
Web：[POST /prompt → 写回 .runtime/jobs/job-002.json]
     [剪贴板复制"继续"]
画师（回 CC）：[Cmd+V + Enter]
Skill：[读最新 prompt → 调 Lovart → PNG 落地 → 写 jobs/done → 推 Web]
画师：[Web 图廊看 4 张图，觉得第 2 张氛围不对]
       [反馈框输"2 号那张光线再阴一点"，点提交]
Web：[POST /feedback → 写 .runtime/draft/<ts>.md]
     [剪贴板复制"继续"]
画师（回 CC）：[Cmd+V + Enter]
Skill：[读 draft → 改 prompt → 出图 → 推 Web]
...
```

**核心 UX 节奏**：
- **画师 90% 时间在 Web**（看图、编 prompt、改 spec、写反馈）
- **画师每轮触发只需 Cmd+V + Enter**（剪贴板已就绪，1.5 次按键操作）
- **Skill 不感知"是 Web 触发还是终端触发"**——它每次 turn 起始都重新读文件，所以从它视角看就是文件变了

### 4.4 剪贴板桥接机制（B+ 核心创新点）

**问题**：v1.0 C 阶段需要画师"切到 CC 窗口、用键盘打字'继续'"——这是最大的 UX 摩擦。

**B+ 解法**：浏览器 Clipboard API + 一条用户教育信息。

| 步骤 | 实现 | 说明 |
|---|---|---|
| Web 提交后写文件 | `POST /xxx` 落盘 | 仍是文件系统作为 source of truth |
| 自动复制 "继续" | `navigator.clipboard.writeText('继续')` | 浏览器原生 API，HTTPS / localhost 可用 |
| Toast 提示 | 顶部固定条："已保存。切到 CC 按 `Cmd+V` `Enter`" | 强提示让画师形成肌肉记忆 |
| 画师动作 | 切窗口 + 粘贴 + 回车 | 1.5 次按键（粘贴 + 回车），切窗口在 Mac 是 Cmd+Tab |

**降级方案**：

| 失败场景 | 降级 |
|---|---|
| Clipboard API 失败（浏览器禁权限）| toast 内容改为可点击的"复制'继续'"按钮 |
| 画师老忘记切窗口 | viewer-server 启动时打印"提示：Web 改完后切回这里 Cmd+V Enter"到 CC 控制台，每次 Skill turn 结束也提示一次 |
| 画师就是不愿切 | V2 议题：探索 CC IDE 集成 API 是否能从 Web 触发 turn |

**为什么不做"Web 直接触发 Skill"**：

- CC 没有"文件变化时触发 turn"的 hook（只有 `PreToolUse` / `Stop` 等基于 turn 生命周期的 hook）
- 自己起 daemon 轮询文件 → 调 Claude Agent SDK：要画师配 API key、装 daemon、不再用画师自己的 Claude 账号——违反"零部署、零账号"原则
- 切 OpenClaw / 其他 host：今天画师装 CC 已经是事实，再换 host 等于推倒重来
- **结论**：剪贴板桥接是当下成本最低、约束最少的解。1.5 次按键的画师摩擦，远小于其他方案的开发或运维代价

### 4.5 显式不做（B+ 阶段）

| 不做 | 原因 |
|---|---|
| inbox 队列 + lock + 时间戳排序 | B+ 的 Web 写动作直接落盘到对应文件（`characters/*.md`、`jobs/*.json`、`draft/*.md`），不需要排队 |
| Web 主动反推 Skill | 剪贴板 + Cmd+V Enter 已经够低摩擦 |
| 多用户 / 账号 / 鉴权 | localhost 单画师 |
| 图片精细对比（slider）| V2 议题 |
| 历史版本回溯 UI | Git 已经够 |
| WebSocket（双向持久连接）| SSE 单向推送已足够（服务端→客户端变化通知）；WebSocket 引入额外握手和状态管理复杂度，不值得 |
| Web 直接触发 Claude API | 画师用自己 Claude 账号 + Skill 在 CC 跑，Web 不持任何 token |
| 自动切换出图模型 | Lovart 已聚合多模型；UI 切模型 V3 |

### 4.6 验收标准（v2.1 回填）

B+ 上线后 4 周内必须全部达成。任何一项失败 → 触发 v2.x 修订。

**M1 · 画师独立完成度（硬指标）**
画师在上线 2 周内独立完成 ≥ 3 个完整角色（从 spec 编写到 4 张终选图通过画师自评 ≥ 7/10），全程**不需要项目负责人介入解释概念或排查问题**。介入 1-2 次（教学性质）可接受；介入 3+ 次 = 失败。

**M2 · 触发回合摩擦（硬指标）**
"Web 保存 → CC 触发下一轮"的 90 分位耗时 ≤ 5 秒，剪贴板桥接 7 天滑动窗口成功率 ≥ 95%。失败时降级方案（toast 内"复制'继续'"按钮）必须 100% 可点。

**M2 测量机制**（v2.2 补充）：Web 每次尝试写剪贴板后，立即 `POST /clipboard-attempt` 上报结果（`{ts, success: true/false, reason}`）；server.log 追加该记录。上线 1 周后统计 `success` 字段得出成功率。

**M3 · 可观测性可用性（硬指标）**
画师报告任何"Web 改完但 Skill 没反应"问题时，能在 30 分钟内仅凭画师电脑上的 `.runtime/jobs/*.json` + Web 元信息面板截图复现并定位根因。无法定位 = 失败。

**M4 · 画师主观评分曲线（学习曲线指标）**
上线第 1 周末 + 第 4 周末各做一次 4 题 1-10 分自评：
1. UI 顺手度（看图、编 prompt、改档案的总体感受）
2. 触发回合的认知负担（Cmd+V Enter 是肌肉记忆 还是 心智负担）
3. 看图/对比图体验（vs Finder + Preview）
4. 比"直接跟 LLM 聊天"是否更高效

**通过条件**：第 4 周综合分 ≥ 7 **且** 高于第 1 周综合分（学习曲线收敛而非放弃）。第 4 周低于第 1 周 = 触发设计反思。

**M3 实现说明**：见 §4.2 交付物 9（图片元信息面板）。每张图自带的 json + Web 面板代替传统日志，画师能直接截图发问题。

---

## 5. 文件模型修订（取代 v1.0 §5）

### 5.1 项目目录结构

```
game-ui-ai-workflow/
├── characters/                     # 角色档案（Git 追踪）
├── design-system/                  # 风格基线（Git 追踪）
├── memory/                         # 画师偏好（Git 追踪）
├── docs/                           # 文档（Git 追踪）
├── skill/                          # Skill 内核 + viewer-server（Git 追踪）
└── .runtime/                       # 运行时（gitignore）
    ├── config.json                 # 画师本地配置
    ├── jobs/                       # 出图任务状态
    │   └── job-001.json
    ├── draft/                      # 画师反馈草稿（Web 写、Skill 读完即移走）
    │   └── 20260515-143012.md
    ├── draft-processed/            # 已读 draft 归档
    └── server.pid

<image_storage_root>/               # 画师本地路径（不进 Git）
```

**相对 v1.0 的变化**：

- `inbox/` → **`draft/`**（语义更准：画师在 Web 上写的"草稿反馈"，Skill 下次 turn 起始读取）
- 取消复杂的 inbox lock / 时间戳排序：每个 draft 文件独立、Skill 一次性全读、读完移到 `draft-processed/`

### 5.2 谁读谁写矩阵

| 文件 / 目录 | Skill 读 | Skill 写 | Web 读 | Web 写 |
|---|---|---|---|---|
| `characters/*.md` | ✅ | ✅ | ✅ | **✅** |
| `<image_storage_root>/**/*.png` | ❌ | ✅（调 Lovart 落地）| ✅ | ❌ |
| `memory/character-prefs.md` | ✅ | ✅ | ✅ | **✅** |
| `design-system/visual-style.md` | ✅ | ❌（画师手改）| ✅ | ❌ |
| `.runtime/config.json` | ✅ | 首次 ✅ | ✅ | ✅ |
| `.runtime/jobs/*.json` | ✅ | ✅ | ✅ | **✅（改 prompt）** |
| `.runtime/draft/*.md` | ✅ | ❌ | ❌ | **✅** |
| `.runtime/draft-processed/*.md` | ❌ | ✅（处理后移动）| V3 审计 | ❌ |

### 5.3 Skill 主循环锚点

每次 Skill turn 起始**必做**：

1. 读 `.runtime/draft/*.md`，按文件名升序合并到当前上下文
2. 把读过的 draft 移到 `.runtime/draft-processed/`
3. 检查 `characters/<active>.md` 时间戳是否新于上次读取——是则重读

这一个三步骤是"Web 写 → Skill 读"的全部桥接逻辑。**没有 inbox 队列、没有 lock、没有 SSE**。

### 5.4 Skill ↔ Web 并发写约定（v2.1 新增）

文件系统始终以**落盘版本**为准（"本地优先"）。Web 表单编辑期间 Skill 写入同一文件时不强制中断画师，采用非破坏式同步策略。

**Web 端机制**：

| 概念 | 行为 |
|---|---|
| 表单状态 | "未保存的工作副本"，**不等于**落盘文件 |
| 自动保存 | 表单变更后 5s 静默落盘（不触发剪贴板）|
| 手动保存 | "保存"按钮立即落盘 + 自动复制"继续"到剪贴板 + toast 提示 |
| 检测落盘变更 | 每 2s 轮询文件 mtime/hash；发现"文件内容比表单上次同步时新" → 顶部**小字提示"内容已变更，[刷新]"**，**不自动覆盖画师未保存草稿** |

**Skill 端机制**：
- Skill turn 内自由读写 `characters/*.md`、`.runtime/jobs/*.json`
- Skill 不持锁，假设画师配合"Web 表单看到刷新提示时主动决策"

**冲突解决**：
- Skill 写 → Web 检测到 → 提示画师 → 画师选择"刷新"（丢草稿用 Skill 版本）或"保存"（用画师版本覆盖）
- 罕见竞态（画师点保存的瞬间 Skill 也在写）→ **last writer wins**；Web 下次 2s 轮询会发现内容变化再次提示
- **不引入文件锁**。代价：极小概率画师看到的不是最新落盘状态，由提示机制兜底

**为什么不锁**：v1.0 的 inbox lock 在 v2.0 已经被否定。锁带来的复杂度（残锁清理、跨进程语义、画师误操作）远大于"罕见竞态被提示兜底"的代价。

---

## 6. 三个澄清记录（office-hours 决策依据）

### 6.1 "全程 Web 操作 = 等于做了个网站产品 = 找开源改？"

❌ 类别错误。

| 你产品的真正核心 | 市面上有现成的吗 |
|---|---|
| Web 聊天 + 出图渲染 | ✅ ComfyUI / Open WebUI / Lobe Chat 一抓一把 |
| 角色档案规范、画师偏好长期沉淀、Skill 可复用 | ❌ 没有，是本项目独有的护城河 |

开源 chat front-end 是 5w+ 行的复杂项目，把"角色档案规范 + 长期沉淀"塞进去 = 拆它的内部数据模型重写。**自己写 localhost web app 的真实成本：Server 100-200 行 + 前端 200-300 行**。比改开源便宜得多，且不背上游版本包袱。

### 6.2 "Skill 对话回到终端，从网页怎么传回？"

剪贴板桥接（见 §4.4）。这是当下成本最低、约束最少的解。其他三条路（CC file hook / OpenClaw / CC IDE API）都因为不可行或违反"零部署、零账号"原则被淘汰。

### 6.3 "Skill 数据本地 / 网站数据线上，怎么抉择？"

不存在这个抉择。**B+ 里的 Web 是 localhost**（画师本机的 5173 端口），数据全在画师本地文件系统，不出本机。"零部署、零账号、零云"哲学锚 100% 保留。"线上"是 V3 SaaS 议题，B+ 不碰。

---

## 7. 图片存储路径配置

沿用 v1.0 §6 完全不动。

---

## 8. 风险清单更新

| # | 风险 | 严重度 | 触发条件 | 缓释 |
|---|---|---|---|---|
| 1 | 画师对"Cmd+V + Enter"形成习惯需要时间 | 中 | 上线第 1 周 | (1) 浏览器 toast 强提示；(2) CC 控制台同步打印提示；(3) 画师指南视频 demo |
| 2 | 浏览器 Clipboard API 在某些环境失败 | 中 | 浏览器隐私模式 / 旧版 Safari | toast 内显示可点击的"复制'继续'"按钮兜底 |
| 3 | Web 写 spec 时 Skill 同时跑 | 低 | 时序竞态 | Skill turn 结束才让出控制权；Web 写直接覆盖文件，下次 turn 起始时读新版 |
| 4 | 共享盘 / 网盘掉线 | 中 | 网络故障 | Skill 写图前 health check，失败时缓存到 `.runtime/images-pending/` |
| 5 | 画师误删 `.runtime/` | 中 | 画师手动清理 | (1) `.runtime/` 加 README 警示；(2) 周期性备份到 `.runtime-archive/`；(3) 关键状态也写一份到 Git 内的角色档案 |
| 6 | Lovart API 配额 / 成本 | 中 | 批量出图 | (1) 出图前估算 token + 美元成本告诉画师；(2) 单次出图量上限（默认 4 张）|
| 7 | 跨场景视觉一致性不达标 | 中 | GPT Image 2 不稳定 | 已写进功能说明书 §三 / §十 |
| 8 | 端口 5173 被占用 | 低 | 画师另一个项目也开了 vite | 自动找空端口 |

**v1.0 的"画师拒绝在 CC 里聊天"风险**：因 v2.0 画师主体不在 CC，此风险**降级为低**——画师只需 Cmd+V + Enter。

**v1.0 的"回合衔接 UX 摩擦"风险**：因剪贴板桥接，**降级为风险 1**（习惯养成期问题，非结构问题）。

---

## 9. 已知 TODO（当下不解决但要登记）

| 项 | 说明 | 决策窗口 |
|---|---|---|
| ~~B+ 验收标准~~ | ~~进入实施前回填~~ → **v2.1 §4.6 已回填** | ✅ 已闭环 |
| 画师培训物料 | 上手指南、视频 demo（重点：Cmd+V Enter 触发动作）| 实施期间 |
| 图片目录命名规范 | 对接 `design-system/naming.md` | 实施期间 |
| Lovart 成本上限 | 团队预算 + 单次出图量 | 上线前 |
| 剪贴板桥接降级阈值 | 画师容忍度调研：连续几次失败后切换到手动按钮兜底 | 上线后 1 周 |
| 图片元信息字段定义 | §4.2 交付物 9 涉及的 json 字段、可编辑字段白名单、editor 校验规则 | 实施期间 |
| **Web 内独立"出图板块"**（v2.1 新登记 / D5b 议题）| 画师在 Web 内选模型、配 API key、手动触发出图。**B+ 不做**，原因：违反"零账号、零云"哲学锚 + 与 Skill 主路径职责重叠。等 B+ 上线、画师真实使用 4 周后评估是否真有需求 | V2 |
| **/plan-design-review 跳过决策**（v2.1 登记）| CEO Review 时识别 §4.2 三栏布局缺信息层级 / 状态覆盖（loading/empty/error）/ 图廊交互模式。用户选择在进 writing-plans **之前手动跑** /plan-design-review 补齐 | writing-plans 之前 |
| 开源协议 | MIT / Apache / GPL | V3 议题 |

---

## 10. 底线坚持（产品哲学锚）

沿用 v1.0 §9 完全不动：

1. 文件系统是唯一 source of truth
2. Skill 内核纯 markdown + tool calls，不绑死任何 agent host
3. 画师写动作的最终归宿是文件
4. 画师本地有完整权力（PS / Finder / VS Code 绕过 UI 直改）
5. 零部署、零账号、零云（V1 + V2 全周期）

---

## 11. V3 议题（B+ 之后，超出本设计范围）

沿用 v1.0 §10 不动 + 新增：

- **开源**：把 Skill + Web UI 包成 npm / pip 包发布
- **多模型切换**：画师在 Web 设置里选模型
- **多 agent host 适配**：OpenClaw / Codex
- **从 localhost 走到云**：SaaS 化
- **CC IDE 集成 API**（v2.0 新增）：探索 CC 是否提供"从外部 trigger turn"的能力，能取代剪贴板桥接，做到"Web 完全无感"

---

## 12. 下一步

- 用户审稿本设计文档 v2.0
- 审核通过后，进入 **writing-plans** 阶段，**仅拆 B+ 单阶段的可执行实施计划**：
  - 8 个交付物拆解为可执行任务
  - 每个任务的验证标准
  - 任务依赖图与并行机会
  - B+ 验收标准回填
  - 技术栈选定（Node vs Python、React vs Vue、监听库等）
  - 剪贴板桥接的浏览器兼容性矩阵
- B+ 上线、画师真实使用 1-2 周后再立 V3 议题，避免远期需求过早 freeze

---

**版本**：v2.2（2026-05-15）
**取代**：v1.0（2026-05-14）
**承接**：功能说明书 v0.2 §产品形态与交互层（待定项）
**下一版触发**：进入实施后若发现 B+ 假设失真（剪贴板桥接画师真不接受、Skill 主循环锚点跑不通、SSE 在目标环境有兼容问题），回填修订

---

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | 范围与策略 | 2 | ✅ CLEAR | HOLD SCOPE，12 个决策，5 项文档修订，2 项进 TODOS |
| Outside Voice | Claude subagent | 独立挑战逻辑漏洞 | 1 | issues_found → fixed | 初始评分 5/10；5 个问题中 3 个已通过 D10/D11/D12 修复（SSE 架构、clipboard 日志、元信息面板保留）|
| Eng Review | `/plan-eng-review` | 架构与测试（必须）| 0 | ❌ 未运行 | — |
| Design Review | `/plan-design-review` | UI/UX 细节 | 0 | ⏳ 待用户手动运行 | 用户计划在进 writing-plans 前运行 |
| DX Review | `/plan-devex-review` | 开发者体验 | 0 | — | — |

- **OUTSIDE VOICE**：初始 5/10 → 应用 D10/D11/D12 后主要问题已解决。剩余小问题（多 tab clipboard 竞争、"继续"语义单一、端口动态发现）已登记在 §9 TODO 或 writing-plans 阶段处理
- **CROSS-MODEL**：两个模型对"image metadata panel 是否 YAGNI"存在分歧；用户明确选择保留（用户上下文胜出）
- **UNRESOLVED**：0 个未决策
- **VERDICT**：CEO Review CLEAR + Outside Voice 主要问题已修复 — **eng review required before writing-plans**
