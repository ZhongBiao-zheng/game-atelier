# 网站、本机数据与工坊 MCP

> 状态：分阶段开发，已实现 P1a 身份握手与 P1b Host / Origin 边界；网站、会话鉴权与 MCP 工具尚不可用。本次改造只提交 PR，不合并。
> 代码核验基线：`42fcc49`；核验日期：2026-08-31。本文不是已上线功能说明。

## 目标与边界

用户打开网站，在明确连接本机服务后使用 Atelier；项目、画布、生成记录与媒体仍保存在自己的
`data_root`。外部 Codex / Claude 可以加载已有工作流 Skill，通过受限 MCP 工具准备任务；用户批准后，
由同一个本机服务执行生成，并在工坊中看到结果。

“数据在本地”指持久化归属，不代表数据永不出机：生成时选中的提示词和参考素材会发给模型供应商，
Agent 读取的上下文会进入所用 Agent 的会话。网站代码在获得连接授权后也能读取获授权的本机内容，
因此网站本身是需要信任的软件，不是一个无权限的静态外壳。

本次不做：云端项目数据库、账号同步、跨设备访问本机、云中转、本地网络扫描、任意 shell / 文件代理、
网站内嵌完整 Agent IDE、自动运行画布或后台无人批准的付费生成。保留现有本地页面启动方式，
不把数据改存 IndexedDB，不重做存储格式，不改供应商协议与价格。

## 两条入口，一个执行中心

```text
HTTPS 网站 ── 用户配对 ──┐
本地页面 ── 同源会话 ───┼─→ viewer-server (127.0.0.1)
                       │     ├─ 项目 / 画布 / 媒体 API → data_root
Codex / Claude         │     └─ 工坊服务 → 人工批准 → Job Runner → 供应商
  └─ Workflow Skill    │                                  └─ 本机产物 + Job
      └─ stdio MCP ────┘
```

不另起一套保存服务、生成队列或供应商调用层。MCP 适配器只翻译协议和返回结果，不直接写 Job、
读密钥或调用供应商；浏览器只提交意图，不能声明自己已获批准。连接契约见
[本机连接](contracts/local-connection.md)，工具与批准契约见[工坊 MCP](contracts/workshop-mcp.md)。

## 当前实现与需要补齐的差距

| 已核验的代码 | 当前行为 | 改造要求 |
| --- | --- | --- |
| `src/viewer_server/server_app.py` | 绑定回环地址，但没有覆盖全部 API 的会话鉴权 | Host、Origin、身份、能力按顺序校验，新增端点默认拒绝 |
| `src/viewer_server/server.py` | 基线用 `/api/config` 探活；P1a 已替换为状态握手和实例核验 | 后续配对 / 协议协商继续复用握手，不把完整配置当探活接口 |
| `web/src/api/http.ts` 及页面 | 请求、媒体 URL 和直接 `fetch` 分散 | 统一连接地址、鉴权、失效和取消语义；不能只改一个请求封装 |
| `web/src/hooks/useSSE.ts` | `EventSource('/events')`，同源假设 | 带鉴权的事件流；撤销和换连接时立即停止旧流 |
| `routes.py` 的 key / raw / gallery / download 路由 | 本地页面可管理配置并取媒体 | 原始密钥与本机设置仅本地管理；媒体和下载也必须鉴权 |
| `POST /api/jobs/{id}/confirm` | 仅将待确认任务改为待执行，Skill 负责启动 | 服务端批准、冻结、调度形成一个可靠流程 |
| `job_runner.run_job` | 接受 `PENDING_CONFIRM`，执行时自行转 `PENDING` | 运行器不接受未批准任务；原 CLI / Skill 路径一起更新，不留下绕过入口 |
| `Job.runner_started_at` | schema 限定只用于 Canvas | 不直接借给工坊；工坊执行声明与恢复规则单独建模、复用现有锁 |
| `skills/*/SKILL.md`、工作流 CLI | 工坊上下文与操作规则完整，但偏直接文件 / CLI 调用 | 保留生产规则，新增受限工具路径；不复制一套提示词、模型或归档逻辑 |

## 模块职责

以下是目标边界，不要求逐项创建一个类或目录。

- **本机连接模块（viewer-server）**：运行实例、网站配对、本地会话、Agent 授权、撤销、路由能力检查、
  媒体凭据。它不能修改项目内容或决定生成参数。
- **Web 连接层**：React 原生状态保存连接状态；统一 JSON、上传、下载、媒体、SSE 与取消。
  页面不读令牌、不拼接本机地址。业务 API 继续维护自己的输入输出类型。
- **工坊服务（Python 共享业务层）**：显式目标解析、上下文、文档修订、准备生成、冻结输入、
  幂等、批准与调度。Web / MCP / 工作流 CLI 在各自授权入口后调用它。
- **MCP 适配器**：官方 SDK 的 stdio 工具表；只调用工坊 HTTP 能力，不提供通用 HTTP 或文件工具。
- **Job Runner 与 callers**：保留实际生成、供应商任务 ID、计费记录、产物校验和落盘职责。
- **Workflow Skill**：负责对话、创作步骤、参数解释与人工反馈；不是权限证明或后台执行器。

## 产品交互

### 网站连接

未连接时只显示“连接本机”和启动说明，不出现空项目列表，也不把连接失败表现成数据丢失。
本机启动入口可打开网站并带上无凭证的回环地址；用户在本地页面确认网站来源，复制一次性连接码，
到网站完成配对。浏览器自身的本地网络权限提示仍由用户批准，产品不绕过。

连接成功后进入现有界面。正常态只有一个低干扰的连接入口，展开后查看目标、断开或重连；
配置密钥、切换数据目录、管理授权都跳回本地管理页，不给网站发送原始密钥。
连接错误在连接面板内给出具体原因，不在画布或每个节点重复堆文案。

同一运行实例仍只允许一个交互式编辑页面；新页面显式接管前，旧页面有机会保存或导出草稿。
不能静默清空草稿或让两个编辑器同时持有写权限。本地授权管理页不是第二个编辑器。

### 工坊 Agent

用户在本地授权一个 Agent 会话访问选定项目，再从 Codex / Claude 使用 Skill。
Agent 先取得带目标 ID 的上下文，按工作流整理内容、准备生成；Atelier 展示目标、模型、
参考素材、数量、参数与预计费用，用户点击批准后执行。Agent 可查询状态和读取已授权结果。

第一版的付费批准放在 Atelier 页面，而不是相信模型传来的 `confirmed: true`。
这是拟定的安全交互，会随 PR 一并供用户评审；不以“已安装 Skill / 已连接 MCP / 对话里写了同意”
冒充服务端可核验的批准。无浏览器后台自动出图不属于本版。

## 分阶段交付与 PR 依赖

| 阶段 | 可单独验证的交付 | 退出条件 |
| --- | --- | --- |
| P0 文档 | 本文、两份契约、两条 ADR、术语与本地任务清单 | 现状与目标分开；权限、数据流、批准、恢复、验收明确 |
| P1 连接地基 | 本机实例握手、鉴权、授权管理、统一 Web 传输；先保持本地页面可用 | 全部数据 API 有能力归属，媒体 / SSE / 原生配置无旁路；本地现有流程回归通过 |
| P2 网站闭环 | 网站配对、连接状态、媒体与下载、一次启动入口、独立静态构建 | 真实 HTTPS 网站连接回环服务完成上传→生成→保存→刷新；撤销立即生效 |
| P3 工坊服务 | 类型化上下文 / 文档 / 生成准备；批准和调度由服务端拥有 | Web 与 CLI 不再绕过批准；断线、重复提交、重启不会悄悄重发已计费请求 |
| P4 MCP + Skill | stdio 工具、显式授权、工作流 Skill 的工具路径、客户端配置示例 | Codex 与 Claude 各走通角色、UI、视频的受控流程；结果回到原有工坊 |
| P5 整体验收 | 跨平台、安全、真实页面、安装与升级检查 | 记录实际通过的浏览器 / OS / Agent 版本、未通过项和演示步骤，等待用户审阅 |

P1 同时供 P2、P3 使用；P4 依赖 P3。每阶段单独 PR，按依赖设置 stacked PR 的 base，
让差异只包含本阶段；不为继续开发而提前合并。与“版本号 + GitHub”PR #76 独立，保留其未合并状态。
测试站按用户指定的 Vercel 免费方案准备，先使用平台域名；具体账号 / 项目在部署前确认，
不购买域名或付费资源。测试站只分发界面，不能把本机服务挪到 Vercel Functions。

P1a 已实现最小状态 API、启动记录实例校验、启动互斥和有界探测；未启用完整网站协议，
握手明确返回 `protocol: null`。这是 P1 的第一条可验证切片，不表示鉴权或网站已完成。

P1b 在读取请求体和路由处理前，校验实际监听端口对应的 Host、精确 Origin 与浏览器 Fetch Metadata，
覆盖 API、媒体、SSE、文档与静态资源。不开放 CORS；普通本机页面与原生本机请求继续可用。
这不是会话鉴权：无浏览器来源头的原生本机请求仍可访问现有 API，身份、能力登记与撤销尚待 P1 后续完成。
SDK 已安装并锁定 2.1.1，仅验证独立 stdio fixture；没有注册全局 MCP 或开放工坊工具。

### 开发开始前的授权门槛

- 用户于 2026-08-31 明确允许新增官方 Python MCP SDK；使用项目依赖 `mcp`，不另起 Node 服务。
  安装时核验真实可获取版本与现有依赖兼容性，并提交锁文件；这不代表 MCP 工具已可用。
- 用户已明确允许新增连接鉴权、MCP 和前端连接流程的回归测试；实现同步写测试，尤其覆盖批准与恢复，
  不以人工点通替代权限 / 重放 / 并发的自动回归。
- Vercel 测试项目尚待部署时确定，不阻止代码与本地验证；本次不自动发布公网预览。
- 不更改用户现有 Codex / Claude 配置，不安装全局 MCP；客户端验收使用临时配置与隔离数据目录。

## 验收与发布规则

1. 数据仍在同一个 `data_root`；关闭网站 / Agent 后文件和已批准任务不依赖浏览器存活；
   清理网站缓存不删除文件，但会断开连接。静态站点不包含真实项目、密钥、运行目录或诊断日志。
2. 恶意 Origin、DNS rebinding Host、无凭证 API / SSE / 媒体、过期或重复配对、跨项目 ID、
   伪造确认、并发批准、旧修订覆盖、符号链接逃逸全部拒绝且不产生付费副作用。
3. 断网、切换本机地址、撤销、服务器重启和跨版本连接都有确定状态；旧响应不能写入新连接。
   不静默降级到不鉴权、不依赖关闭浏览器安全设置。
4. 大视频使用 Range 与原生媒体流，不能为了鉴权把全部素材一次性拉成 Blob；图库分页及媒体缓存有上限。
5. 重启不会重复计费：持有供应商 task ID 时仅续查；请求是否送达不明时标记待核对，禁止自动重发。
   不承诺外部供应商无法保证的“严格 exactly-once”。
6. 每个代码 PR 跑 `make verify`；UI 改动做真实页面验证。付费调用用 fake provider / fixture，
   不用用户密钥做自动回归；真实付费联调需另获明确授权。
7. 桌面 Chrome / Edge 的真实 HTTPS→回环链路为首个验证目标；Safari / Firefox 不在通过前宣称支持。
   Windows / macOS / Linux 分别验证启动、文件权限、锁与重启，不能把 CI 单测当浏览器真机验收。
8. 不自动合并、不切换用户现用服务、不推送网站生产部署；用户审阅后才决定合并及发布顺序。

## 核验依据

- Infinite Canvas 的[本地 Agent 桥](https://github.com/basketikun/infinite-canvas/blob/main/canvas-agent/README.md)
  展示了网站与本地进程分离的路径。这里只借鉴传输结构，不照搬权限和浏览器存储。
- 浏览器访问回环服务还受到 [Chrome Local Network Access](https://developer.chrome.com/blog/local-network-access)
  的权限与安全上下文约束；仅添加旧式 PNA / CORS 响应头不等于完成浏览器接入。
- [官方 Python MCP SDK](https://github.com/modelcontextprotocol/python-sdk) 支持 stdio；
  [PyPI](https://pypi.org/project/mcp/) 在核验时展示 2.1.1。实现时复核支持版本并提交锁文件，
  不使用旧教程的导入方式冒充已验证实现。
- 信任与会话设计同时参考 [MCP 安全指南](https://modelcontextprotocol.io/docs/tutorials/security/security_best_practices)。

本地执行记录与任务状态放 `.scratch/local-workspace-mcp/`；拟定长期决定见 ADR-0015 / ADR-0016，
具体端点以链接的契约为准，不在多个文档重复维护字段定义。
