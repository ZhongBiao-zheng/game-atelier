# Domain Docs

本仓库采用单一领域上下文。

## 探索代码前读取

- 根目录 `CONTEXT.md`：领域术语与明确禁用的同义词。
- `docs/adr/`：与当前改动相关的架构决策。

文件不存在时继续工作,不预先创建空文档。只有术语或难以逆转的决策真正确定后,才通过 `domain-modeling` 补充。

## 使用规则

- Ticket、测试、方案和代码命名使用 `CONTEXT.md` 中的标准术语。
- 新概念与现有词汇冲突时先指出,不静默创造同义词。
- 方案与 ADR 冲突时必须明确标注并重新确认,不能把旧决策悄悄覆盖。
